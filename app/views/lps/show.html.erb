<p id="notice"><%= notice %></p>

<div class="row justify-content-center">
  <table class="table table-hover table-bordered col-sm-4 content-center">
    <thead><tr><th colspan="3"><%= @lp.artist.name %>'s LP</th></tr></thead>
    <tbody class="table-striped">
      <tr>
        <th>Name:</th>
        <td><%= @lp.name %></td>
      </tr>
      <tr>
        <th>Year:</th>
        <td><%= @lp.year %></td>
      </tr>
      <tr>
        <th>Genre:</th>
        <td><%= @lp.genre %></td>
      </tr>
			<tr>
				<td colspan="2" class="text-center">
          <%= link_to 'Edit', edit_lp_path(@lp), class: "btn btn-primary" %>
          <%= link_to 'Back', lps_path, class: "btn btn-secondary" %>
				</td>
			</tr>
    </tbody>
  </table>
</div>
