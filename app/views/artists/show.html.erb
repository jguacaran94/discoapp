<p id="notice"><%= notice %></p>

<div class="row justify-content-center">
  <table class="table table-hover table-bordered col-sm-4 content-center">
    <tbody>
      <tr>
        <th>Name:</th>
        <td><%= @artist.name %></td>
      </tr>
      <tr>
        <th>Description:</th>
        <td><%= @artist.description %></td>
      </tr>
      <th colspan="2"><%= link_to 'LP list', lps_artist_path, class: "btn btn-link-hover" %></th>
			<tr>
				<td colspan="2" class="text-center">
					<%= link_to 'Edit', edit_artist_path(@artist), class: "btn btn-primary" %>
					<%= link_to 'Back', artists_path, class: "btn btn-secondary" %>
				</td>
			</tr>
    </tbody>
  </table>
</div>
